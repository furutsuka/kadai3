<h3>User info</h3>

<table>
  <tr>
    <td><%= attachment_image_tag @user, :profile_image, :fill, 100, 100, format: 'jpeg', class: "img-circle pull-left profile-img"%></td>
  </tr>
  <tr>
  　　  <td>name</td>
    　<td><%= user.name %></td>
  </tr>
  <tr>
  　　  <td>introduction</td>
  　　<td><%= user.introduction %></td>
  </tr>
  <tr>
  　　<td><% if user.id == current_user.id %></td>
    <td><p><%= link_to "プロフィール編集", edit_user_path(user), class: "pull-right" %></p></td>
  </tr>
    <% end %>
  </table>
</div>