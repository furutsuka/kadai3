<h3>New book</h3>
          <%= form_for book do |f| %>
            <div class=“book-form”>
              <label for=“book_title”><h4>Title</h4></label>
              <%= f.text_field :title %>
            </div>
            <div class=“book-form”>
              <label for=“book_body”><h4>Opinion</h4></label>
              <%= f.text_area :body %>
            </div>
            <div class=“row”>
              <%= f.submit 'Create Book'%>
            </div>
          <% end %>
